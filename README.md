# Global Trends Pipeline

Pipeline to ingest, clean, and harmonize global datasets (conflict, refugees, weather, etc.) into weekly and monthly time series.

## Usage

```bash
pip install -r requirements.txt
python -m src.run_pipeline
